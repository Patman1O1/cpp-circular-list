#ifndef COLLECTIONS_CIRCULAR_LIST_HPP
#define COLLECTIONS_CIRCULAR_LIST_HPP

// ISO C Includes
#include <cstddef>

// ISO C++ Includes
#include <memory>

namespace collections {
    template<typename T, typename Allocator = std::allocator<T>>
    class circular_list {
    };
} // namespace collections

#endif // #ifndef COLLECTIONS_CIRCULAR_LIST_HPP
